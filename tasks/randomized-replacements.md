# Randomized Replacements

Introduce an option to generate replacements randomly (with deterministic seeding) instead of sequential counters. This should produce non-guessable tokens and avoid predictable sequences. Ensure reversibility by persisting the mapping and optionally allow configuring entropy/character set.
