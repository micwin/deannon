# Collision Repair

When two hints or manual entries produce the same anonymized token, we need detection and remediation. Implement collision checks that reassign a new token and update existing files/configs, or at least warn user and block the run.
