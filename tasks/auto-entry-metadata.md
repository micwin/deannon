# Record Hint Metadata on Auto Entries

Generated `full.*` entries should note which hint created them (e.g., `source_hint=namespaces`). This helps auditing and makes it easier to clean/remove stale entries. Extend the INI format or add companion metadata to track provenance.
